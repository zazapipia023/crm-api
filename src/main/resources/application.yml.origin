spring:
  application:
    name:
  flyway:
    enabled: true
    locations: classpath:db/migration
  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true